{% extends "base.html.twig" %}

{% block body %}
	<h1>Ajout d'une catégorie :
	</h1>
	{{ form_start(form) }}
	{{ form_widget(form.name) }}
	<button type="submit">Add a category</button>
	{{ form_end(form) }}
	<a href="{{ path("category_index") }}">Voir les catégories</a>
{% endblock %}
